# H20 Golden Regression Fixtures

This fixture set is driven by `scripts/run-golden-fixtures.sh`.

The runner creates ignored local planning and implementation runs, validates
requested artifact generation, exercises negative paths, records local evidence,
and removes generated run directories before exiting.
