# Crawl4AI Market Discovery Adapter Module

## Purpose

Crawl4AI Market Discovery Adapter turns a Boss Idea market query pack into
public-source discovery and crawl evidence that can feed the existing market
research collector.

The adapter exists because executive brainstorming often needs rapid competitor
and solution discovery, not only manually curated search results. BIR-10 is
therefore a two-stage adapter:

1. query-to-URL discovery through a controlled public web search provider;
2. Crawl4AI-backed page crawl and markdown capture for the selected public URLs.

It still preserves the Boss Idea authority model: search and crawled output are
evidence only, research artifacts must cite sources, and implementation requires
approved manifest artifacts.

## Scope

Active scope:

- consume `agentic/runs/<run-id>/market-research-query-pack.yaml`;
- discover candidate public URLs for each query through an approved search
  provider adapter;
- crawl only public, policy-approved URLs through Crawl4AI or a strictly safer
  compatible crawler;
- normalize Crawl4AI markdown output into the existing
  `boss-idea-market-search` result contract;
- support local fixture HTML for deterministic golden tests;
- enforce DNS, redirect, robots, rate, page count, timeout, content size, and
  evidence storage limits;
- keep raw crawl output under ignored run evidence;
- call `scripts/collect-boss-idea-research.sh` only after result normalization
  passes validation.

Deferred scope:

- paid market databases;
- social media firehose search;
- authenticated crawling;
- customer-internal or private docs;
- hosted crawler fallback such as Firecrawl;
- hosted LLM extraction;
- automatic roadmap, budget, or implementation approval.

## Workflow

```text
boss idea run
  -> collect-boss-idea-research.sh --dry-run
  -> market-research-query-pack.yaml
  -> approved search provider adapter
  -> candidate public URL list
  -> crawl-boss-idea-market.sh
  -> Crawl4AI public page crawl
  -> ignored raw markdown evidence
  -> normalized search-results.yaml
  -> collect-boss-idea-research.sh
  -> validated market-research.md
  -> decision memo / feasibility / POC-MVP planning
```

The adapter does not choose final competitors by itself. It creates a cited,
reviewable candidate source set that Market Research Lead and Staff+ reviewers
can accept, reject, or supplement.

A Boss Idea research path is incomplete unless it includes either:

- at least one crawl/search-sourced result produced by this adapter; or
- a manifest-recorded Staff+ waiver explaining why live/public crawl was not
  appropriate for the idea.

## Artifact Schema

Input artifact:

- `agentic/runs/<run-id>/market-research-query-pack.yaml`
- generated by `scripts/collect-boss-idea-research.sh --dry-run <run-id>`
- contains `queries[]` with `id`, `query`, and `purpose`

URL discovery artifact:

- `agentic/runs/<run-id>/market-candidate-urls.yaml`
- BIR-10 implementation defines
  `agentic/schemas/boss-idea-market-candidate-urls.schema.yaml`
- records `query_id`, `url`, `title`, `snippet`, `provider`, and
  `retrieved_at`
- may be produced by a live search provider, a local fixture provider, or an
  explicitly approved safer crawler/search tool
- must not include private, authenticated, customer-internal, or paywalled URLs

Normalized output artifact:

- `agentic/runs/<run-id>/market-search-results.yaml`
- conforms to `agentic/schemas/boss-idea-market-search.schema.yaml`
- each result records:
  - `id`
  - `query_id`
  - `title`
  - `source_type`
  - `access_date`
  - `reference`
  - optional `url`
  - `signal`
  - short summarized `claim`

Ignored raw evidence:

- `agentic/runs/<run-id>/crawl4ai/raw/<source-id>.md`
- `agentic/runs/<run-id>/crawl4ai/crawl-log.yaml`
- raw evidence must never be copied into tracked docs.

## CLI / Manifest / Pipeline Contract

Command contract:

```bash
scripts/crawl-boss-idea-market.sh --dry-run <run-id>
scripts/crawl-boss-idea-market.sh <run-id> --from-query-pack --search-provider <provider> --output agentic/runs/<run-id>/market-search-results.yaml
scripts/crawl-boss-idea-market.sh <run-id> --seeds <seeds.yaml> --output agentic/runs/<run-id>/market-search-results.yaml
```

Expected contract:

- requires an existing Boss Idea planning manifest;
- requires `boss_idea_intake`;
- reads query pack generated by `collect-boss-idea-research.sh --dry-run`;
- normal production path uses `--from-query-pack --search-provider <provider>`;
- `--seeds` is for deterministic fixtures, replay, and emergency manual
  evidence capture only;
- refuses URLs that are not `http` or `https`;
- refuses localhost, link-local, private RFC1918, metadata-service, and
  non-allowlisted internal targets;
- writes Crawl4AI raw markdown only under ignored run paths;
- writes normalized search results YAML;
- invokes `collect-boss-idea-research.sh` as the required follow-on step unless
  `--results-only` is explicitly supplied for debugging;
- records crawl metadata in `boss_idea_market_crawl` manifest metadata;
- does not approve artifacts, decisions, budget, or implementation.

Manifest metadata shape:

```yaml
boss_idea_market_crawl:
  provider: fixture | <approved-live-provider>
  mode: fixture | live | seed_replay
  crawl4ai_version: <pinned-version>
  candidate_urls_path: agentic/runs/<run-id>/market-candidate-urls.yaml
  results_path: agentic/runs/<run-id>/market-search-results.yaml
  raw_evidence_path: agentic/runs/<run-id>/crawl4ai/raw/
  crawl_log_path: agentic/runs/<run-id>/crawl4ai/crawl-log.yaml
  live_smoke_evidence_path: agentic/reviews/boss-idea-response/bir-10/live-smoke-<date>.md
  waiver:
    reviewer: <staff-plus-role-or-user>
    reason: <why-live-public-crawl-is-not-appropriate>
    scope: <idea-or-run-scope>
    expires_at: <yyyy-mm-dd>
```

`waiver` is omitted when crawl/search-sourced evidence exists. A waiver is
valid only when `reviewer`, `reason`, `scope`, and `expires_at` are present.

Search provider contract:

- provider id is explicit in CLI input and manifest metadata;
- provider returns URL candidates only, not final research claims;
- provider credentials, if any, are read from environment variables and never
  written to tracked files;
- credential environment variables must use
  `BOSS_IDEA_SEARCH_<PROVIDER>_*`, with the common key names
  `BOSS_IDEA_SEARCH_<PROVIDER>_API_KEY`,
  `BOSS_IDEA_SEARCH_<PROVIDER>_BASE_URL`, and
  `BOSS_IDEA_SEARCH_<PROVIDER>_TIMEOUT_SECONDS`;
- provider output is stored under ignored run evidence;
- default deterministic provider is `fixture`;
- the only pre-approved provider for deterministic validation is `fixture`;
- any new live provider requires Staff Security Engineer and Staff Software
  Architect approval before it can ship;
- live provider execution requires `BOSS_IDEA_LIVE_CRAWL=1`;
- if `BOSS_IDEA_LIVE_CRAWL` is not set, the command must refuse public network
  search/crawl and use fixture/local inputs only.
- `fixture` and `--seeds` must not be combined with
  `BOSS_IDEA_LIVE_CRAWL=1`; live runs must use an approved live provider.

Hermes action:

```text
id: crawl_boss_idea_market
mode: planning
authorization.action: artifact.generate
required_inputs: run_id, output
optional_inputs: search_provider, seeds, results_only
```

Hermes action details should mirror BIR-09 conventions: reads the planning
manifest, query pack, optional seed file, and adapter policy; writes ignored
crawl evidence, normalized results, downstream research artifact, and manifest
metadata. Pipeline registration must list the command in executable validation
and must not make live network crawl part of default golden fixtures.

## Crawl4AI Contract

Use Crawl4AI as a local-first crawler. Official Crawl4AI documentation describes
`AsyncWebCrawler` as the async crawler API and documents that it fetches pages
and can produce markdown output through crawler configuration. It also exposes
the `crwl` CLI for simple crawling and markdown output.

Implementation should prefer the Python API over shelling out to `crwl` when
we need strict per-URL policy enforcement, structured result capture, and
testable dependency injection.

Required runtime controls:

- `BrowserConfig(headless=True)`;
- `CrawlerRunConfig` with explicit timeout and cache policy;
- Crawl4AI version pinned in dependency metadata and recorded in the manifest;
- Crawl4AI upgrades require Staff+ review before use in production discovery;
- markdown output only;
- no LLM extraction strategy;
- no schema or DOM extraction strategy unless separately approved by Staff+
  review;
- no automatic provider API tokens;
- max pages per query;
- max bytes or markdown characters per page;
- max redirect depth;
- TLS verification enabled; ignore-certificate-errors is forbidden;
- deny-by-default host allowlist created from the approved candidate URL set;
- one concurrent crawl per host;
- minimum delay between requests to the same host;
- robots.txt evaluation before crawl unless an explicit Staff+ waiver is
  recorded;
- stable user agent format:
  `agentic-delivery-boss-idea-crawler/<version> (+mailto:<contact>)`;
- structured crawl log with success, skipped, and failed URLs.

Default policy knobs:

- max candidate URLs per query: 10, hard cap 20;
- max crawled pages per query: 5, hard cap 10;
- max crawled pages per run: 30, hard cap 50;
- max redirect depth: 3;
- page timeout: 20 seconds, hard cap 30 seconds;
- max response body before markdown conversion: 2 MiB;
- max retained markdown characters per page: 120,000;
- per-host concurrency: 1;
- minimum per-host delay: 2 seconds;
- abort after 5 consecutive policy blocks or 10 total crawl/search failures in
  one run unless an explicit Staff+ waiver raises the limit for that run.

URL safety must run before every network request and after every redirect.
Implementation must resolve hostnames, reject private or metadata-service
addresses, and defend against DNS rebinding by checking the connect-time
resolved address or an equivalent browser routing policy. Redirect targets must
be validated with the same policy before follow-up requests.

## Failure Behavior

Block crawling when:

- run id is invalid;
- manifest is missing or not `boss-idea-response`;
- query pack is missing or malformed;
- live mode is requested without `BOSS_IDEA_LIVE_CRAWL=1`;
- seed URL is not `http` or `https`;
- URL resolves to private, loopback, link-local, multicast, or metadata-service
  address;
- hostname resolves to a safe public IP initially but changes to a blocked IP
  class before connection;
- redirect target violates scheme, host, IP, allowlist, or max-depth policy;
- TLS certificate validation fails;
- host is not in the per-run allowlist;
- `robots` or site policy disallows crawl for the selected path;
- per-host rate or concurrency policy would be exceeded;
- the run reaches the configured consecutive policy-block threshold or total
  crawl/search failure threshold;
- page exceeds content size or timeout limits;
- content truncation would be needed but cannot be recorded in the crawl log;
- Crawl4AI returns no usable markdown;
- Crawl4AI returns DOM, JavaScript-extracted, schema-extracted, or LLM-extracted
  content instead of markdown-only output;
- normalized results lack competitor or mainstream-practice signals;
- claim text violates BIR-09 collector limits: multiline, more than 280
  characters, unsupported URL scheme, more than 12 consecutive words copied
  from one raw source, or more than 20 percent token overlap with one raw
  source after stop-word removal;
- raw evidence would be written to tracked paths.

Escalate rather than block when:

- crawl returns too few sources but there is at least one cited public source;
- a relevant source requires authentication;
- source freshness is insufficient and needs human caveat;
- competitor naming is ambiguous.

## Validation Strategy

Validation must include:

- bash syntax for wrapper scripts;
- Python unit tests for URL policy and result normalization;
- fixture crawl using local HTML or `raw://` input, not the public internet;
- negative tests for SSRF targets:
  - `http://localhost`
  - `http://127.0.0.1`
  - `http://169.254.169.254`
  - private RFC1918 addresses
  - public hostname resolving to a private address
  - public seed redirecting to a private or metadata-service address
  - path traversal in output paths
- negative test for TLS verification failure;
- robots.txt allow and disallow fixtures;
- per-host rate and max-redirect-depth tests;
- deny-by-default allowlist test;
- user-agent format test;
- redirect-chain DNS-rebinding test;
- content truncation log test;
- markdown-only Crawl4AI output test that rejects DOM, JavaScript, schema, or
  LLM extraction output;
- negative tests for excessive page count and content length;
- negative tests for claim length, multiline claim, more than 12 consecutive
  copied words, and more than 20 percent token overlap with one raw source;
- negative tests for missing required signals;
- validation of generated `market-search-results.yaml`;
- validation of downstream `market-research.md`;
- validation that raw crawl evidence paths are ignored and not tracked;
- privacy scan over tracked files;
- AIT plus Claude Code review.

Default golden fixtures must not depend on external network availability.
Live crawl smoke tests must require both `--live` and
`BOSS_IDEA_LIVE_CRAWL=1`, and they must be excluded from default validation.

## Test Cases

Positive tests:

- local fixture page normalizes to a competitor signal;
- local fixture page normalizes to a mainstream-practice signal;
- fixture search provider maps query ids to candidate URLs;
- generated search results pass `collect-boss-idea-research.sh`;
- raw markdown evidence stays under ignored run directory;
- manifest records crawl metadata without approving artifacts.

Negative tests:

- missing query pack fails;
- missing search provider or seed URLs fail;
- unsupported scheme fails;
- localhost and metadata-service URLs fail;
- DNS rebinding to blocked IP fails;
- redirect to blocked IP fails;
- non-allowlisted host fails;
- robots disallow fails;
- TLS verification failure fails;
- excessive redirect depth fails;
- redirect-chain DNS rebinding to blocked IP fails;
- per-host rate violation fails;
- user-agent that does not match
  `agentic-delivery-boss-idea-crawler/<version> (+mailto:<contact>)` fails;
- oversized page fails;
- truncated content without an explicit crawl-log truncation record fails;
- DOM, JavaScript, schema, or LLM extraction output from Crawl4AI fails;
- generated result with missing reference fails;
- generated result with bad source type fails;
- generated result with missing required signal fails;
- output outside run directory fails.

## Acceptance Criteria

- Boss Idea run can produce a query pack, discover candidate public URLs, and
  crawl policy-approved public pages through Crawl4AI or a strictly safer
  compatible crawler/search tool.
- Crawl output is normalized into the existing market-search result schema.
- Normalized results can feed `collect-boss-idea-research.sh` without manual
  editing.
- Live crawl is optional and never required for deterministic golden fixtures.
- Live crawl is required for production-grade boss idea competitor discovery
  unless a Staff+ waiver is recorded in the manifest.
- SSRF, DNS rebinding, redirect, TLS, robots, rate, and unsafe URL classes are
  blocked before Crawl4AI receives or follows the URL.
- Raw crawl evidence remains ignored and public-safe summaries remain tracked
  only when explicitly generated.
- Validation asserts raw evidence paths remain ignored and that no raw
  Crawl4AI output is accidentally tracked.
- No crawl or search output can approve artifacts, decisions, or implementation.

## Doc Review Standard

Claude Code review must verify:

- Crawl4AI role is limited to local-first public-source evidence gathering;
- live web crawling is not part of default deterministic validation;
- the design does not imply hosted LLM extraction or Firecrawl fallback;
- URL policy, output path, and raw-evidence boundaries are explicit;
- query-to-URL discovery is in scope, not deferred to an unnamed future slice;
- the adapter feeds the existing BIR-09 collector contract.

## Code Review Standard

Implementation review must verify:

- URL policy runs before network access;
- DNS/IP checks block private and metadata-service targets, including redirect
  and DNS-rebinding cases;
- robots, TLS, redirect, rate, page, and content limits are enforced;
- timeouts, page limits, and content limits are enforced;
- Crawl4AI errors are recorded without leaking raw output to tracked files;
- generated results pass `collect-boss-idea-research.sh`;
- golden fixtures use local HTML or controlled fixture inputs;
- live crawl smoke tests are opt-in and clearly labeled.
- live crawl smoke tests run manually before enabling or upgrading a live
  provider, are owned by the Staff Platform Engineer with Market Research Lead
  review, and record public-safe evidence under
  `agentic/reviews/boss-idea-response/bir-10/live-smoke-<date>.md`.

## Rollback

Rollback must remove:

- `scripts/crawl-boss-idea-market.sh`;
- any Python helper used only by this adapter;
- Crawl4AI-specific fixtures;
- Hermes action and pipeline references;
- ignored crawl output under affected runs.

Existing BIR-09 collector and validator should remain intact because they are
provider-neutral and useful without Crawl4AI.

## References

- Crawl4AI Quick Start: https://docs.crawl4ai.com/core/quickstart/
- Crawl4AI CLI Guide: https://docs.crawl4ai.com/core/cli/
- Crawl4AI SDK Reference: https://docs.crawl4ai.com/complete-sdk-reference/
- Existing ADR: `docs/adr/002-crawl4ai-doc-ingestion.md`

## Review Expectations

Review must confirm that Crawl4AI improves research speed without weakening
source traceability, copyright safety, network safety, or manifest authority.
